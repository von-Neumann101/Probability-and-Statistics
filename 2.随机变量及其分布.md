# 离散随机变量分布律
## 泊松分布
**定义**：设随机变量 $\boldsymbol { X }$ 所有可能取的值为 $0 \,, 1 \,, 2 \,, \cdots$ ，而取各个值的概率为
$$
P \{ X = k \} = \frac { \lambda ^ { k } \mathrm { e } ^ { - \lambda } } { k! }, \; k = 0, 1, 2, \cdots,
$$
其中 $\lambda > 0$ 是常数.则称 $X$ 服从参数为入的泊松分布，记为 $X \sim \pi ( \lambda )$
$\lambda$指的是在单位时间内的事件的发生次数（对比二项分布的期望$\mathbb E[X]=np$），泊松分布将其取为定值。

**泊松定理**：设 $\lambda > 0$ 是一个常数， $n$ 是任意正整数，设 $n p _ { n } \! = \! \lambda$ ，则对于任一固
定的非负整数 $k$ ，有
$$
\operatorname* { l i m } _ { n \to \infty } { \binom { n } { k } } p _ { n } ^ { k } \, ( 1 - p _ { n } ) ^ { n - k } \! = \! \frac { \lambda ^ { k } \, \mathrm { e } ^ { - \lambda } } { k \,! }.
$$
二项分布描述$n$次独立试验中成功$k$次的概率；泊松分布描述在单位时间、单位区域内，某种稀有事件发生$k$次的概率。

当$n\ge 20,p\le0.05$的时候泊松分布可以很好的近似二项分布
# 连续型随机变量及其概率密度
## 分布函数
定义设 ${ X }$ 是一个随机变量， ${ x }$ 是任意实数，函数
$$
F ( x ) \! = \! P \! \left\{ X \! \le \! x \right\}, - \infty \! < \! x \! < \! \infty
$$
称为 ${ X }$ 的分布函数.

**性质**：
1. 单调不减
2. $0\le F(x)\le 1$
3. $F(-\infty)=0,F(\infty)=1$
4. $F(x+0)=F(x)$（为了在分段点取等，必须是右连续）
反之，满足以上四个性质的函数也是某个随机变量的分布函数
## 连续型随机变量
如果对于随机变量 $X$ 的分布函数$F ( x )$ ，存在非负可积函数 $f ( x )$ ，使对于任意实数 $x$ 有
$$
F ( x ) = \int _ { - \infty } ^ { x } f ( t ) \mathrm { d } t,
$$
则称 $X$ 为连续型随机变量， $f ( x )$ 称为 $X$ 的**概率密度函数**，简称概率密度.

对于随机型连续变量$X$来说，有限点概率的和是0（$P\{X=a\}=0$），故也有
$$
P ( a { < } X { \leq } b ) \! = \! P ( a { \leq } X { \leq } b ) \! = \! P ( a { < } X { < } b ).
$$
## 分布律
### 均匀分布
若连续型随机变量 $X$ 具有概率密度
$$
f(x)=  
\begin{cases}  
\dfrac{1}{b-a}, & a<x<b,\\[6pt]  
0, & \text{其他}.  
\end{cases}
$$
则称 $X$ 在区间 ${ ( a, b ) }$ 上服从均匀分布，记为 $X { \sim } U ( a, b )$ .
### 指数分布
若连续型随机变量 $X$ 的概率密度为
$$
f(x)=  
\begin{cases}  
\dfrac{1}{\theta}\mathrm e^{-x/\theta}, & x>0,\\[6pt]  
0, & \text{其他}.  
\end{cases}
$$
其中 $\theta > 0$ 为常数，则称 $X$ 服从参数为 $\theta$ 的指数分布.

**性质**（无记忆性）：若随机变量$X$服从指数分布，那么$P\{X>s+t\mid X>s\}=P\{X>t\}$
### 正态分布
若连续型随机变量 $X$ 的概率密度为
$$
f ( x ) \! = \! \frac { 1 } { \sqrt { 2 \pi } \sigma } \mathrm { e } ^ { - \frac { ( x - \mu ) ^ { 2 } } { 2 \sigma ^ { 2 } } } \,, \quad - \infty \! < \! x \! < \! \infty \,,
$$
其中 $\mu, \! \sigma ( \sigma \! > \! 0 )$ 为常数，则称 $X$ 服从参数为 $\mu, \sigma$ 的正态分布或高斯（Gauss）分布，记为 $X \! \sim \! N ( \mu, \sigma ^ { 2 } )$ 。特别地，当$\mu=0,\sigma=1$时称随机变量$X$服从标准正态分布.
$f(x)$在$x=\mu\pm\sigma$处有拐点
**归一化**：若随机变量$X$服从正态分布，那么$Z=\dfrac{X-\mu}{\sigma}\sim\mathcal N(0,1)$
# 随机变量的函数分布
**定理**：设随机变量 $X$ 具有概率密度 $f _ { x } \left( x \right), - \infty { < } x { < } \infty$，又设函数$g(x)$处处可导且恒有 $g ^ {'} ( x ) \! > \! 0$ （或恒有 $g ^ {'} ( x ) < 0 )$ ,则 $Y \! = \! g \left( X \right)$是连续型随机变量，其概率密度为
$$
f_Y(y)=  
\begin{cases}  
f_X[h(y)]\,|h'(y)|, & \alpha<y<\beta,\\[6pt]  
0, & \text{其他}.  
\end{cases}
$$
其中 $\alpha \! = \! \operatorname* { m i n } \! \left\{ \, g \left( \, - \infty \right), g \left( \infty \right) \, \right\}, \beta \! = \! \operatorname* { m a x } \! \left\{ \, g \left( \, - \infty \right), g \left( \, \infty \, \right) \, \right\}, h \left( \, y \, \right)$ 是 $g ( x )$ 的反函数.
**Example**：
- 常规

![[Pasted image 20260630112330.png|397]]
- 非单调
![[Pasted image 20260630112918.png]]
- 正态分布
随机变量$X\sim\mathcal N(\mu,\sigma^2)$，那么随机变量$Y=aX+b$也服从正态分布

> [!NOTE] 随机变量，分布函数，概率密度函数
> 随机变量通过分布函数关联到概率密度函数，概率密度函数是分布函数的导数


