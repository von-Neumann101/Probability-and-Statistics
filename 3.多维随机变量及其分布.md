# 二维随机变量
**定义**（二维随机变量）：设 $E$ 是一个随机试验，它的样本空间是 $S = \{ e \}$ ,设 $X  = X (  e )$ 和 $Y = Y ( e )$ 是定义在 $S$上的随机变量，由它们构成的一个向量$(X, Y )$叫做**二维随机向量**或**二维随机变量**

**定义**（联合分布函数）：设（ $X, Y )$ 是二维随机变量，对于任意实数 $x, y$ ，二元函数：$$
F ( x, y ) \! = \! P \{ \left( X \! \leqslant \! x \right) \! \cap ( Y \! \leqslant \! y ) \} { \overset { \mathrm { 记为} }   { \longrightarrow } } P \{X\le x, Y\le y\}
$$称为二元随机变量$(X,Y)$的分布函数，或称为随机变量$X$和$Y$的***联合分布函数***

**性质**：
- $F(x,y)$是变量$x,y$的单调不减函数
- $0\le F(x,y)\le 1$
- 对于任意给定的$y$，$F(-\infty,y)=0$；对于任意给定的$x$，$F(x,-\infty)=0$；$F(-\infty,-\infty)=0$；$F(\infty,\infty)=1$
- $F(x,y)$关于$x,y$右连续

**定义**（二维离散型随机变量）：如果二维随机变量 $( X, Y )$ 全部可能取到的值是**有限对或可列无限多对**，则称 $( X, Y )$ 是**二维离散型随机变量**

**定义**：设二维离散型随机变量 $( X, Y )$ 所有可能取的值为 $( \, x _ { i } \,, y _ { j } \, ) \,, i \,, j = 1 \,, 2 \,, \cdots,$ 记
$P \{ X \! = \! x _ { i } \,, Y \! = \! y _ { j } \} = p _ { i j } \,, i \,, j \! = \! 1 \,, \! 2 \,, \! \cdots,$ 则由概率的定义有
$$
p _ { i j } \geq 0 \,, \quad \sum _ { i = 1 } ^ { \infty } \sum _ { j = 1 } ^ { \infty } p _ { i j } \, = \, 1.
$$
我们称 $P \{ X { = } x _ { i } \,, Y { = } y _ { j } \} = p _ { i j } \,, i \,, j { = } 1 \,, 2 \,, \cdots \, j \,,$ 为二维离散型随机变量 $( X, Y )$ 的分布律，或称为随机变量 $X$ 和 $Y$ 的联合分布律.

**定义**（概率密度）：对于二维随机变量$( X, Y )$的分布函数 $F ( x, y )$ ，如果存在非负可积函数$f ( x, y )$使对于任意 $x, y$ 有
$$
F ( x, y ) = \int _ { - \infty } ^ { y } \int _ { - \infty } ^ { x } f ( u, v ) \, \mathrm { d } u \mathrm { d } v,
$$
则称 $( X, Y )$ 是**二维连续型随机变量**，函数 $f ( x, y )$称为二维连续型随机变量$( X, Y )$ 的**概率密度**，或称为随机变量 $X$ 和 $Y$的***联合概率密度***

**性质**：
- $\textstyle f ( x, y ) \geq 0,$
- 设 $G$ 是 $x O y$ 平面上的区域，点 $( X, Y )$ 落在 $G$ 内的概率为（重要）
$$
P \{ ( X, Y ) \in G \} = \iint _ { G } \! \! f ( x, y ) \, \mathrm { d } x \mathrm { d } y.
$$
- 若 $f ( x, y )$ 在点 $( x, y )$ 连续，则有
$$
\frac { \partial ^ { 2 } F ( x, y ) } { \partial x \partial y } \! = \! f ( x, y ).
$$
**近似**：
$$
\operatorname* { l i m } _ { \Delta x \to 0 + \atop \Delta y \to 0 + } \frac { P \{ x < X \leq x + \Delta x, y < Y \leq y + \Delta y \} } { \Delta x \Delta y }=f(x,y)
$$
![[Pasted image 20260702152929.png|427]]

**定义**（$n$维随机变量）：设 $E$ 是一个随机试验，它的样本空间是 $S = \{ e \}$ ,
设 $X _ { 1 } = X _ { 1 } \left( e \right),X _ { 2 } \! = \! X _ { 2 } \left( e \right), \cdots, X _ { n } \! = \! X _ { n } \left( e \right)$ 是定义在 $\mathit { S }$ 上的随机变量，由它们构成的一个 ${ n }$ 维向量 $( X _ { 1 }, X _ { 2 }, \cdots, X _ { n } )$ 称为${ n }$维随机向量或$n$维随机变量.

**定义**：对于任意 ${ n }$ 个实数 $x _ { 1 } \,, x _ { 2 } \,, \cdots, x _ { n } \,, n$ 元函数
$$
F ( x _ { 1 } \,, x _ { 2 } \,, \cdots, x _ { n } ) \! = \! P \langle \, X _ { 1 } \! \leqslant \! x _ { 1 } \,, X _ { 2 } \! \leqslant \! x _ { 2 } \,, \cdots, X _ { n } \! \leqslant \! x _ { n } \, \rangle
$$
称为 $n$ 维随机变量 $( X _ { 1 }, X _ { 2 }, \cdots, X _ { n } )$ 的**分布函数**或随机变量 $X _ { 1 }, X _ { 2 }, \cdots, X _ { n }$的**联合分布函数**.它具有类似于二维随机变量的分布函数的性质.
# 边缘分布
**定义**（边缘分布函数）：二维随机变量$(X,Y)$有分布函数$F(x,y)$，同时随机变量$X,Y$各自也有分布函数$F_X(x),F_Y(y)$，称为二维随机变量$(X,Y)$关于$X,Y$的**边缘分布函数**
$$F_X(x)=\Pr\{X\le x\}=\Pr\{X\le x, Y<\infty\}=F(x,\infty)=\sum_{x_i\le x}\sum_{j=1}^\infty p_{ij}$$
相当于是我们完全不管另外的一个变量了，我们用一个毫无信息量的条件($Y<\infty$)代替

**定义**（边缘分布律）：
$$
\begin{align*}
p_{i\cdot}=\sum_{j=1}^\infty p_{ij}=\Pr\{X=x_i\},\quad i=1,2,\ldots\\
p_{\cdot j}=\sum_{i=1}^\infty p_{ij}=\Pr\{Y=y_j\},\quad j=1,2,\ldots
\end{align*}
$$
我们把所有$y$下标的求和加起来，相当于是消除了这个变量

**定义**（边缘概率密度）：
$$
\begin{align*}
F_X(x)&=F(x,\infty)=\int_{-\infty}^x\left[\int_{-\infty}^\infty f(x,y)dy\right]dx\\
f_X(x)&=\int_{-\infty}^\infty f(x,y)dy\\
f_Y(y)&=\int_{-\infty}^\infty f(x,y)dx\\
\end{align*}
$$
**Example**：
![[Pasted image 20260708160406.png|578]]
![[Pasted image 20260708160423.png|572]]
发现边缘分布律就是行/列的和
**二维正态分布**：
$$
\begin{aligned}
f ( x, y ) = \frac { 1 } { 2 \pi \sigma _ { 1 } \sigma _ { 2 } \sqrt { 1 - \rho ^ { 2 } } } \exp \Bigr \{ \frac { - 1 } { 2 ( 1 - \rho ^ { 2 } ) } \Bigl [ \frac { ( x - \mu _ { 1 } ) ^ { 2 } } { \sigma _ { 1 } ^ { 2 } }
 - \, 2 \rho \, \frac { ( \, x - \mu _ { 1 } \, ) ( y - \mu _ { 2 } \, ) } { \sigma _ { 1 } \sigma _ { 2 } } + \frac { ( y - \mu _ { 2 } \, ) ^ { 2 } } { \sigma _ { 2 } ^ { 2 } }\Bigr ] \Bigr \},
\end{aligned}
$$
容易求出两个边缘分布都是一维正态分布，且不依赖于参数$\rho$
# 条件分布
[[Conditional-Probability]]
$$
\Pr\{X=x_i\mid Y=y_j\}=\dfrac{\Pr\{X=x_i,Y=y_j\}}{\Pr\{Y=y_j\}}=\frac{p_{ij}}{p_{\cdot j}}
$$
**定义**（条件概率密度）：定义设二维随机变量 $( X, Y )$ 的概率密度为 $f ( x, y ), ( X, Y )$关于$Y$的边缘概率密度为 $f _ { Y } ( y )$。若对于固定的 $y, f _ { Y } ( y ) { > } 0$，则称$\frac { f ( x, y ) } { f _ { Y } ( y ) }$为在 $Y \! = \! y$ 的条件下 $X$ 的**条件概率密度**，记为
$$
f_{X\mid Y}(x\mid y)=\dfrac{f(x,y)}{f_Y(y)}
$$
同时，我们有条件分布函数，记为
$$
\Pr\{X\le x\mid Y=y\}=F_{X\mid Y}(x\mid y)=\int_{-\infty}^x\frac{f(x,y)}{f_Y(y)}dx
$$
# 互相独立随机变量
**定义**：设 $F ( x, y )$ 及 $F _ { X } ( x ) \,, F _ { Y } ( y )$ 分别是二维随机变量 $( X, Y )$ 的分布函数及边缘分布函数.若对于所有 $x, y$ 有
$$
\Pr\{X\le x,Y\le y\}=\Pr\{X\le x\}\Pr\{Y\le y\}\Longleftrightarrow F(x,y)=f_X(x)f_Y(y)
$$
则称随机变量$X$和$Y$是相互独立

**Example**：
证明：二维正态随机变量$(X,Y)$，$X$和$Y$相互独立的充要条件是$\rho=0$

![[Pasted image 20260708170739.png|612]]

**推广**：若对于所有的 $x _ { 1 } , x _ { 2 } , \cdots, x _ { m } ; y _ { 1 } , y _ { 2 } , \cdots , y _ { n }$ 有
$F ( x _ { 1 }, x _ { 2 }, \cdots, x _ { n }, y _ { 1 }, y _ { 2 }, \cdots, y _ { n } ) = F _ { 1 } ( x _ { 1 }, x _ { 2 }, \cdots, x _ { n } ) F _ { 2 } ( y _ { 1 }, y _ { 2 }, \cdots, y _ { n } ) \,,$其中 $F _ { 1 } \,, F _ { 2 } \,, F$ 依次为随机变量 $( \, X _ { 1 } \,, X _ { 2 } \,, \cdots, X _ { m } \, ) \,, ( Y _ { 1 } \,, Y _ { 2 } \,, \cdots, Y _ { n } \, )$ 和 $( X _ { 1 }, X _ { 2 }, \cdots,$$X _ { m } \,, Y _ { 1 } \,, Y _ { 2 } \,, \cdots, Y _ { n } \, )$ 的分布函数，则称随机变量 $( X _ { 1 } \,, X _ { 2 } \,, \cdots, X _ { m } )$和 $( Y _ { 1 }, Y _ { 2 }, \cdots, Y _ { n } )$ 是相互独立的.

**定理**：设$( X _ { 1 } \,, X _ { 2 } \,, \cdots, X _ { m } \, )$和$( Y _ { 1 }, Y _ { 2 }, \cdots, Y _ { n } )$相互独立，则 $X _ { i } \, ( \, i \! = \! 1 \,, 2 \,, \cdots,m )$和$Y _ { j } \left( j \! = \! 1 \,, \! 2 \,, \cdots, n \right)$ 相互独立.又若 $h,g$ 是连续函数，则 $h \left( X _ { 1 } \,, X _ { 2 } \,, \cdots, X _ { m } \right)$ 和$g ( Y _ { 1 }, Y _ { 2 }, \cdots, Y _ { n } )$ 相互独立.
# 两个随机变量的函数的分布
## $Z=X+Y的分布$
设 $( X, Y )$ 是二维连续型随机变量，它具有概率密度 $f ( x, y )$。则 $Z \! = \! X \! + \! Y$ 仍为连续型随机变量，其概率密度为
$$
f_{X+Y}(z)=\int_{-\infty}^\infty f(z-y,y)dy=\int_{-\infty}^\infty f(x,z-x)dx
$$
特殊地，当$X$和$Y$独立时，有
$$
f _ { X + Y } ( z ) = \int _ { - \infty } ^ { \infty } f _ { X } ( z - y ) f _ { Y } ( y ) \mathrm { d } y=\int _ { - \infty } ^ { \infty } f _ { X } ( x ) f _ { Y } ( z - x ) { \mathrm { d } } x.
$$
称为$f_X$和$f_Y$的**卷积公式**，记为$f_X*f_Y$
## $Z=\frac YX和Z=XY的分布$
设 $( X, Y )$ 是二维连续型随机变量，它具有概率密度 $f ( x, y )$ ,则 $Z = { \frac { Y } { X } }$和$Z \! = \! X Y$ 仍为连续型随机变量，其概率密度分别为：
$$
f _ { Y / X } ( z ) = \int _ { - \infty } ^ { \infty } \mid x \mid f ( x, x z ) \, \mathrm { d } x,
$$
$$
f _ { X Y } ( z ) = \int _ { - \infty } ^ { \infty } \frac { 1 } { \mid x \mid } f \Big ( x, \frac { z } { x } \Big ) \mathrm { d } x.
$$
特殊地，当$X$和$Y$独立时，有
$$
f _ { Y / X } ( z ) = \int _ { - \infty } ^ { \infty } \mid x \mid f _ { X } ( x ) f _ { Y } ( x z ) \mathrm { d } x.
$$
$$
f _ { X Y } ( z ) = \int _ { - \infty } ^ { \infty } { \frac { 1 } { \mid x \mid } } f _ { X } ( x ) f _ { Y } \Big ( { \frac { z } { x } } \Big ) \mathrm { d } x.
$$
## $M=\max\{X,Y\}和N=\min\{X,Y\}的分布$
注意到关键事实$M\le z\Longleftrightarrow X\le z \land Y\le z$，易得其分布
